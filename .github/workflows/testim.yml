name: Testim E2E
on: [push]

jobs:
    run-testimio-cli:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: actions/setup-node@v4
              with:
                node-version: 'lts'
                # You can also change 'lts' to any supported LTS Node.js version.
            - run: npm install -g @testim/testim-cli
            - run: testim --token "UKtdnJpqHLlg9vOBycbsRiWDz7AxieM6L67pfZ3dQ4mnPlJmVF" --project "BhnXj0QMB9eZJeorzeOV" --grid "Testim-Grid" --mode "extension"
